// Removed — CharacterHealth.Setup() is used by mobs/bosses, not players.
// Player health uses a different setup path that doesn't go through CharacterHealth.
namespace SwornTweaks.Patches
{
    static class PlayerHealthPatch { }
}
